{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "core.defs.schema.json",
  "title": "Core definitions",
  "description": "Shared $defs for every schema in this repository. Referenced as core.defs.schema.json#/$defs/<name>. Scores and rubrics are deliberately absent: consumers that want them extend their own schemas.",
  "$defs": {
    "schema_version": {
      "title": "Schema version",
      "description": "Every document declares its schema version; concrete schemas narrow this to a const.",
      "type": "string",
      "pattern": "^workflows(?:\\.[a-z0-9]+(?:-[a-z0-9]+)*)+\\.v[1-9][0-9]*$"
    },
    "identifier": {
      "title": "Identifier",
      "description": "Stable machine identity. Whitespace-only and empty identifiers are rejected by construction.",
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._/-]*$",
      "minLength": 1,
      "maxLength": 200
    },
    "digest": {
      "title": "Content digest",
      "description": "Lowercase hex SHA-256 with an explicit algorithm prefix.",
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "timestamp": {
      "title": "UTC timestamp",
      "description": "RFC 3339 in UTC; local offsets are rejected so run records sort globally.",
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(?:\\.[0-9]{1,9})?Z$"
    },
    "status": {
      "title": "Step status",
      "type": "string",
      "enum": ["COMPLETED", "FAILED", "BLOCKED", "STOPPED"]
    },
    "criterion_result": {
      "title": "Criterion result",
      "description": "NOT_RUN and INCONCLUSIVE exist so that unchecked criteria can never be reported as passing.",
      "type": "string",
      "enum": ["PASS", "FAIL", "NOT_RUN", "INCONCLUSIVE"]
    },
    "severity": {
      "title": "Finding severity",
      "type": "string",
      "enum": ["CRITICAL", "HIGH", "MEDIUM", "LOW"]
    },
    "finding_status": {
      "title": "Finding status",
      "type": "string",
      "enum": ["OPEN", "RESOLVED", "ACCEPTED_RISK", "WITHDRAWN"]
    },
    "evidence_kind": {
      "title": "Evidence kind",
      "description": "probe marks an executed adversarial check; a negative-path claim requires one.",
      "type": "string",
      "enum": ["command", "probe", "file", "diff", "log", "citation"]
    },
    "evidence_ref": {
      "title": "Evidence reference",
      "description": "One logged piece of evidence. Claims point at these by id.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind", "ref"],
      "properties": {
        "id": { "$ref": "#/$defs/identifier" },
        "kind": { "$ref": "#/$defs/evidence_kind" },
        "ref": {
          "description": "The command line, path, pointer, or citation the evidence came from.",
          "type": "string",
          "minLength": 1,
          "maxLength": 2000
        },
        "digest": { "$ref": "#/$defs/digest" },
        "exit_code": { "type": "integer" },
        "excerpt": { "type": "string", "maxLength": 4000 }
      }
    },
    "evidence_ref_list": {
      "title": "Evidence reference ids",
      "type": "array",
      "maxItems": 100,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/identifier" }
    },
    "finding": {
      "title": "Finding",
      "description": "A defect claim with evidence and the smallest action that resolves it.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "severity",
        "status",
        "claim",
        "evidence_refs",
        "required_action",
        "negative_path_claim"
      ],
      "properties": {
        "id": { "$ref": "#/$defs/identifier" },
        "severity": { "$ref": "#/$defs/severity" },
        "status": { "$ref": "#/$defs/finding_status" },
        "claim": { "type": "string", "minLength": 1, "maxLength": 2000 },
        "location": { "type": "string", "minLength": 1, "maxLength": 500 },
        "evidence_refs": {
          "$ref": "#/$defs/evidence_ref_list",
          "minItems": 1
        },
        "required_action": { "type": "string", "minLength": 1, "maxLength": 1000 },
        "negative_path_claim": {
          "description": "True when the claim asserts that something invalid is rejected. Such claims require an executed probe among the evidence refs.",
          "type": "boolean"
        },
        "lens_id": { "$ref": "#/$defs/identifier" },
        "criterion_id": { "$ref": "#/$defs/identifier" }
      }
    },
    "criterion_outcome": {
      "title": "Criterion outcome",
      "description": "One acceptance criterion mapped to evidence.",
      "type": "object",
      "additionalProperties": false,
      "required": ["criterion_id", "result", "evidence_refs", "negative_path_claim"],
      "properties": {
        "criterion_id": { "$ref": "#/$defs/identifier" },
        "result": { "$ref": "#/$defs/criterion_result" },
        "evidence_refs": { "$ref": "#/$defs/evidence_ref_list" },
        "negative_path_claim": { "type": "boolean" },
        "note": { "type": "string", "maxLength": 2000 }
      }
    },
    "non_claims": {
      "title": "Non-claims",
      "description": "What this document explicitly does not assert. At least one is mandatory: a PASS is bounded by its non-claims.",
      "type": "array",
      "minItems": 1,
      "maxItems": 50,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1, "maxLength": 500 }
    },
    "side_effect_kind": {
      "title": "Side effect kind",
      "type": "string",
      "enum": ["none", "file_write", "process", "network", "git"]
    },
    "side_effect": {
      "title": "Side effect",
      "description": "What the step touched. 'Nothing' is declared explicitly as kind none, never by omission.",
      "type": "object",
      "additionalProperties": false,
      "required": ["kind", "target"],
      "properties": {
        "kind": { "$ref": "#/$defs/side_effect_kind" },
        "target": { "type": "string", "minLength": 1, "maxLength": 500 },
        "note": { "type": "string", "maxLength": 1000 }
      }
    },
    "relative_path": {
      "title": "Repository-relative path",
      "description": "Absolute paths, drive letters, backslashes and parent-directory segments are rejected: a path that can escape the worktree is not a path a gate can reason about.",
      "type": "string",
      "pattern": "^(?!.*(?:^|/)\\.\\.(?:/|$))[A-Za-z0-9._][A-Za-z0-9._/-]*$",
      "minLength": 1,
      "maxLength": 400
    },
    "path_pattern": {
      "title": "Path pattern",
      "description": "A repository-relative path, optionally with * and ** wildcards. Same escape rules as relative_path.",
      "type": "string",
      "pattern": "^(?!.*(?:^|/)\\.\\.(?:/|$))[A-Za-z0-9._*][A-Za-z0-9._/*-]*$",
      "minLength": 1,
      "maxLength": 400
    },
    "git_commit": {
      "title": "Git commit",
      "description": "Full 40-character object name. Abbreviations are ambiguous and are not accepted as a frozen base.",
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "contract_ref": {
      "title": "Contract reference",
      "description": "Which contract, at which revision, with which content. A contract is frozen at run start; a different digest is a different run.",
      "type": "object",
      "additionalProperties": false,
      "required": ["contract_id", "contract_revision", "digest"],
      "properties": {
        "contract_id": { "$ref": "#/$defs/identifier" },
        "contract_revision": { "type": "integer", "minimum": 1 },
        "digest": { "$ref": "#/$defs/digest" }
      }
    },
    "flow_name": {
      "title": "Flow name",
      "type": "string",
      "enum": ["implement", "fanout", "assure", "adjudicate", "benchmark"]
    },
    "step_kind": {
      "title": "Step kind",
      "type": "string",
      "enum": [
        "gate",
        "work",
        "synthesis",
        "review",
        "adjudication",
        "repair",
        "flow",
        "program"
      ]
    },
    "step_state": {
      "title": "Step state",
      "description": "The state a run manifest records for a step. Resume reads this and never repeats a COMPLETED step.",
      "type": "string",
      "enum": ["PENDING", "RUNNING", "COMPLETED", "FAILED", "SKIPPED", "BLOCKED"]
    },
    "verdict_result": {
      "title": "Verdict result",
      "type": "string",
      "enum": ["PASS", "FAIL", "BLOCKED", "INCONCLUSIVE"]
    },
    "ladder_level": {
      "title": "Review ladder level",
      "description": "0 gates, 1 blind recall, 2 calibration, 3 adjudication, 4 cross-family plus human.",
      "type": "integer",
      "minimum": 0,
      "maximum": 4
    },
    "token_usage": {
      "title": "Token usage",
      "description": "New input, cached input and output counted separately. An aggregate that mixes cached and new input overstates cost several-fold, so no field here may be a sum of the others.",
      "type": "object",
      "additionalProperties": false,
      "required": ["new_input", "cached_input", "output"],
      "properties": {
        "new_input": { "type": "integer", "minimum": 0 },
        "cached_input": { "type": "integer", "minimum": 0 },
        "cache_write_input": { "type": "integer", "minimum": 0 },
        "output": { "type": "integer", "minimum": 0 },
        "reasoning_output": { "type": "integer", "minimum": 0 }
      }
    },
    "telemetry": {
      "title": "Call telemetry",
      "description": "One record per model call. dry is required so that a dry run can never be counted as live evidence.",
      "type": "object",
      "additionalProperties": false,
      "required": ["runner", "model", "effort", "dry", "duration_ms", "tokens"],
      "properties": {
        "runner": { "$ref": "#/$defs/identifier" },
        "model": { "type": "string", "minLength": 1, "maxLength": 100 },
        "effort": { "type": "string", "minLength": 1, "maxLength": 40 },
        "dry": { "type": "boolean" },
        "duration_ms": { "type": "integer", "minimum": 0 },
        "tokens": { "$ref": "#/$defs/token_usage" },
        "lens_id": { "$ref": "#/$defs/identifier" },
        "attempt": { "type": "integer", "minimum": 1, "maximum": 100 },
        "retry_reason": {
          "description": "What the previous attempt got wrong. A retry costs a whole call, and an attempt number with no reason cannot tell a fixable prompt or schema problem from model variance.",
          "type": "string",
          "minLength": 1,
          "maxLength": 600
        }
      }
    },
    "candidate": {
      "title": "Candidate identity",
      "description": "What was produced or reviewed, pinned by digest. Provenance (git commit, artifact store) is carried by the referencing schema, not baked in here.",
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "digest", "immutable"],
      "properties": {
        "id": { "$ref": "#/$defs/identifier" },
        "digest": { "$ref": "#/$defs/digest" },
        "immutable": { "type": "boolean" }
      }
    }
  }
}
