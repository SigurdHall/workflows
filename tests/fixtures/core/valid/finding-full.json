{
  "fixture_version": "workflows.fixture.v1",
  "schema": "core.defs.schema.json#/$defs/finding",
  "expect": "valid",
  "reason": "A finding carrying every optional field, including lens attribution.",
  "data": {
    "id": "finding-001",
    "severity": "HIGH",
    "status": "OPEN",
    "claim": "The digest changes when the same set is reordered.",
    "evidence_refs": [
      "probe-reorder"
    ],
    "required_action": "Canonicalize the set before hashing.",
    "negative_path_claim": false,
    "location": "src/example/canonical.py:41",
    "lens_id": "review/determinism",
    "criterion_id": "AC-3"
  }
}
